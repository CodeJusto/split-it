<h1>Split it!</h1>

<% if current_user %>
<h2><%= link_to "Create a new cart", new_cart_path %></h2>


  <% if @carts.size > 0 %>
    <% @carts.each do |cart|  %>
      <%= render 'list_carts', cart: cart %>
    <% end %>
  <% end %>
<% end %>
