   <!-- Only list carts that this user belongs to -->

   <% if cart.cart_roles.select {|t| t.user_id == current_user.id}.length < 1 %>

   <% else %>
     <p><%= cart.name %></p>
     <%= link_to "View cart", cart_path(cart) %>
   <% end %>